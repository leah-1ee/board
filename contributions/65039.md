---
pr-url: https://github.com/nodejs/node/pull/65039
---

## 문제 내용

- 관련 PR: #59891 (원조 패턴), #64760 (작업 중 중복 발견), #64912 (후속)

## 해결 과정과 검증

- `src/node_locks.cc`의 `"shared"`/`"exclusive"` 문자열을 `isolate_data->shared_string()` / `exclusive_string()`로 교체
- `src/node_constants.cc`의 `"errno"` 문자열을 `env->errno_string()`으로 교체 (동작 변화 없음)
- Push 직전 메인테이너의 선반영 커밋(#64760)과 겹치는 2건을 되돌려 스코프를 2개 파일 3줄로 축소, 관련 테스트 회귀 없음 확인
- 최종 변경 2개 파일

## 진행 상태
- 컬래버레이터 승인, `author ready` 라벨 부여, CI 재실행 진행 중
- PR을 참고한 후속 PR(#65081)이 오픈됨
- → Node.js core(C++) 캐싱 문자열 재사용 리팩토링 PR 제출, 컬래버레이터 승인 완료 및 CI 진행 중 🎉

## 기여 회고

Node.js core(src) 레벨 리팩토링에 처음 도전한 PR로, 확인 시점과 반영 시점 사이에도 상태가 바뀔 수 있다는 점을 실전에서 체득했습니다. 중복 커밋을 push 직전 발견해 스코프를 정밀하게 축소한 경험은 이후 조사 절차(사전 중복 체크 표준화)로 이어졌습니다.

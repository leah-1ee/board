---
pr-url: https://github.com/nodejs/node/pull/64912
---

## 문제 내용

- `src/permission/permission.cc`에서 `FIXED_ONE_BYTE_STRING(isolate, "...")`으로 매번 새로 문자열을 만드는 부분이, 같은 파일 안 다른 곳에서는 이미 캐시된 접근자(`permission_string()`/`resource_string()`)를 쓰고 있는 걸 발견했습니다. 예전에 비슷한 패턴을 정리한 선행 PR(#59891)이 있었는데 이 파일은 그 범위에서 빠져 있었던 것 같아서, 나머지도 통일시키는 후속 작업으로 PR을 올렸습니다.

---
pr-url: https://github.com/nodejs/node/pull/65015
---

## 해결 과정과 검증

`validateParams()`의 객체 검사를 `kValidateObjectAllowArray` 플래그를 쓴 `validateObject`로 바꿨습니다. 이 플래그가 기존 조건과 케이스별로 일치합니다. 배열은 계속 통과하고 `null`과 나머지 원시값은 계속 거부됩니다. 기존 검증 테스트가 덮지 않던 배열 허용 경계를 고정하는 테스트를 추가했습니다.

---
pr-url: https://github.com/nodejs/node/pull/64976
---

## 문제 내용

- `Node.js` 공식 문서 `doc/api/crypto.md`를 검토하던 중, 암호 키 파생 함수의 동기·비동기 버전 간 타입 문서 불일치 확인.
- `crypto.pbkdf2()`와 `crypto.scrypt()`는 `password`·`salt` 인자로 `ArrayBuffer`를 받는다고 명시됨. 동기 버전인 `pbkdf2Sync()`와 `scryptSync()`에는 해당 타입이 누락 → 추가
